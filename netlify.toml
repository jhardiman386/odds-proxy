# ============================================================
# 🧱 Build Configuration
# ============================================================
[build]
  functions = "netlify/functions"

# ============================================================
# ⚙️ Function Bundling
# ============================================================
[functions]
  node_bundler = "esbuild"

# ============================================================
# 🕒 Scheduled Jobs
# ============================================================

# Existing 12-hour roster refresh job (optional legacy)
[scheduled."roster-refresh"]
  cron = "0 */12 * * *"
  function = "router"

# New unified maintenance scheduler (rosters + odds + props pre-cache)
[scheduled."scheduler"]
  cron = "0 */6 * * *"
  function = "router"