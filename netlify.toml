# ============================================================
# 🧱 BUILD CONFIGURATION
# ============================================================
[build]
  functions = "netlify/functions"
  publish = "dist"
  command = "npm run build"

# ============================================================
# ⚙️ FUNCTION SETTINGS
# ============================================================
[functions]
  node_bundler = "esbuild"
  included_files = ["netlify/functions/router.js", "netlify/functions/health.js"]
  external_node_modules = ["node-fetch"]

# ✅ Node version must be declared in an environment object
[functions.environment]
  NODE_VERSION = "18"

# ============================================================
# 🔁 ROUTING
# ============================================================
[[redirects]]
  from = "/.netlify/functions/*"
  to = "/.netlify/functions/:splat"
  status = 200

# ============================================================
# 🕒 SCHEDULED JOBS
# ============================================================
[scheduled."roster-refresh"]
  cron = "0 */12 * * *"
  function = "router"

[scheduled."scheduler"]
  cron = "0 */6 * * *"
  function = "router"

[scheduled."health-check"]
  cron = "*/30 * * * *"
  function = "health"

# ============================================================
# 🌍 HEADERS
# ============================================================
[[headers]]
  for = "/.netlify/functions/*"
  [headers.values]
    Access-Control-Allow-Origin = "*"
    Access-Control-Allow-Headers = "Content-Type"
    Access-Control-Allow-Methods = "POST, OPTIONS"
